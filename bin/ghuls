#!/usr/bin/env ruby
# encoding: utf-8

$LOAD_PATH.unshift(File.dirname(File.realpath(__FILE__)) + '/../cli/lib')

require_relative '../cli/lib/ghuls'
require 'benchmark'

cli = GHULS::CLI.new
cli.run
